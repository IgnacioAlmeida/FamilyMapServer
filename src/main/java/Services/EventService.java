package Services;

import Requests.EventRequest;
import Responses.EventResponse;

/**
 * An Event Service.
 */
public class EventService {

    /**
     * Returns an event from a person's life with the specified ID.
     * @param eventRequest
     * @return response
     */
    public EventResponse event(EventRequest eventRequest){
        EventResponse response = null;

        return response;
    }

    /**
     * Returns all events for all family members of the current user.
     * @param eventRequest
     * @return response
     */
    public EventResponse allEvent(EventRequest eventRequest){
        EventResponse response = null;

        return response;
    }

}
